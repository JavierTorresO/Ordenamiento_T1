\documentclass[12pt, a4paper]{article}

% --- Paquetes ---
\usepackage[utf8]{inputenc}
\usepackage[spanish]{babel}
\usepackage[margin=2.5cm]{geometry}
\usepackage{amsmath, amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{float}
\usepackage{multirow}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

% --- Configuración de Colores para Código ---
\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\lstset{
    commentstyle=\color{codegreen},
    keywordstyle=\color{magenta},
    numberstyle=\tiny\color{codegray},
    stringstyle=\color{codepurple},
    basicstyle=\ttfamily\small,
    breaklines=true,
    captionpos=b,
    numbers=left,
    showstringspaces=false
}

% --- Título ---
\title{Tarea 1: Algoritmos de Ordenamiento Secuenciales y Paralelos}
\author{Javier Torres 
\& Juan Umaña 
\& Benjamin Jimenez 
\\  \small \textbf{Repositorio:} \url{https://github.com/JavierTorresO/Ordenamiento_T1.git}
\\ \small Sistemas Distribuidos y Paralelos 
\\ \small Universidad de Concepción
}
\date{Abril 2026}

\begin{document}

\maketitle


\section{Introducción}
El problema del ordenamiento es fundamental en la computación paralela, 
sirviendo como caso de estudio para el balance de carga, la granularidad y 
la gestión de memoria. En este trabajo, se exploran estrategias para minimizar el 
\textit{Span} de los algoritmos clásicos, pasando de una recursión paralela simple a una implementación 
completa que paraleliza incluso la operación de mezcla (\textit{merge}).

\section{Descripción de Algoritmos}

\subsection{Mergesort Secuencial y Paralelo}
El Mergesort se basa en el paradigma de divide-and-conquer. 
La versión paralela implementada utiliza tareas 
(\textit{tasks}) de OpenMP para procesar las llamadas recursivas de forma concurrente. 
Se introduce un parámetro de 
\textbf{granularidad} para evitar el sobrecosto de creación de hilos en subproblemas pequeños.

\subsection{Merge Paralelo por Ranks}
Para superar el cuello de botella del merge secuencial de $O(n)$, 
se implementó el algoritmo de \textbf{Merge Path}. 
Este utiliza búsqueda binaria para calcular los 
\textit{ranks} (puntos de corte) en los subarreglos ordenados, 
permitiendo que múltiples hilos fusionen tramos independientes de forma simultánea.

\subsection{K-way Mergesort}
A diferencia del Mergesort binario, 
el K-way divide el arreglo en $k$ bloques. 
La versión paralela completa combina la división en $k$ partes con el merge paralelo interno, 
maximizando el aprovechamiento de los núcleos disponibles.

\section{Análisis Teórico}

\subsection{Modelo PRAM y Work/Span}
Considerando un arreglo de tamaño $n$ y $p$ procesadores:
\begin{itemize}
    \item \textbf{Mergesort Paralelo (Merge Secuencial):}
    \begin{itemize}
        \item $W(n) = O(n \log n)$
        \item $S(n) = S(n/2) + O(n) = O(n)$
    \end{itemize}
    \item \textbf{Merge Paralelo por Ranks:}
    \begin{itemize}
        \item $W_{merge}(n) = O(n)$
        \item $S_{merge}(n) = O(\log n)$
    \end{itemize}
    \item \textbf{Versión Paralela Completa:}
    \begin{itemize}
        \item $S(n) = S(n/k) + O(\log n) = O(\log^2 n)$ (para $k$ constante).
    \end{itemize}
\end{itemize}

\subsection{Teorema de Brent}
Según el Teorema de Brent, 
el tiempo de ejecución estimado es $T_p \leq \frac{W(n)}{p} + S(n)$. 
En nuestra implementación completa, esto se traduce en $T_p \approx \frac{n \log n}{p} + \log^2 n$, 
lo que predice una alta eficiencia para valores grandes de $n$.

\section{Metodología Experimental}

\subsection{Entorno de Ejecución}
Las pruebas se realizaron en un procesador AMD Ryzen 5 8600G (6 núcleos físicos, 12 hilos) con 32 GB de RAM DDR5. 
Se utilizó el compilador `g++` con el flag de optimización `-O3` y el soporte para OpenMP `-fopenmp`.

\subsection{Parámetros de Prueba}
\begin{itemize}
    \item Tamaños de arreglo: $n \in \{2^{20}, 2^{22}, 2^{24}, 2^{26}\}$.
    \item Hilos: $p \in \{1, 2, 4, 8\}$.
    \item Granularidad: $G \in \{512, 1024, 4096, 8192, 16384, 32768\}$.
\end{itemize}

\clearpage

\section{Resultados}

\begin{table}[H]
    \centering
    \caption{Rendimiento de los algoritmos paralelos utilizando $p=8$ hilos frente a su ejecución base ($p=1$) para un tamaño de arreglo $N = 2^{26}$.}
    \label{tab:rendimiento_n26}
    \resizebox{\linewidth}{!}{%
    \begin{tabular}{@{}lcccc@{}}
        \toprule
        \textbf{Algoritmo} & \textbf{Tiempo $T_1$ (s)} & \textbf{Tiempo $T_8$ (s)} & \textbf{Speedup ($S_8$)} & \textbf{Eficiencia ($E_8$)} \\ \midrule
        Par\_Mergesort (Mezcla Sec.) & 6,68 & 1,48 & 4,51 & 0,56 \\
        Par\_Kway (Mezcla Seq.)     & 7,79 & 1,52 & 5,09 & 0,63 \\
        \textbf{Versión Paralela Completa} & 8,68 & 1,76 & 4,92 & 0,61 \\ \bottomrule
    \end{tabular}
    }
\end{table}

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
    title={Impacto de la Granularidad en Mergesort Paralelo ($N=2^{26}, p=8$)},
    xlabel={Granularidad (Elementos - Escala Logarítmica)},
    ylabel={Tiempo de Ejecución (s)},
    xmode=log,
    log basis x=2,
    xmin=256, xmax=65536, % Damos un poco de aire a los bordes
    ymin=1.6, ymax=1.95, % Ajustado más cerca de los valores reales
    xtick={512, 1024, 4096, 8192, 16384, 32768},
    log ticks with fixed point, % Muestra 512 en vez de 2^9
    xticklabel style={rotate=45, anchor=east}, % Evita superposición
    ymajorgrids=true,
    xmajorgrids=true,
    grid style={dashed, gray!40}, % Grilla más suave y elegante
    width=12cm, % Ligeramente más ancho
    height=7cm,
    scaled x ticks=false
]

\addplot[
    color=blue!80!black, % Un azul un poco más oscuro
    mark=square*,
    mark options={fill=white}, % Marcadores limpios
    line width=1.2pt
    ]
    coordinates {
    (512, 1.644)
    (1024, 1.889)
    (4096, 1.699)
    (8192, 1.871)
    (16384, 1.670)
    (32768, 1.775)
    };
\end{axis}
\end{tikzpicture}
\caption{Variación del tiempo de ejecución en función del umbral de corte de OpenMP. Se emplea una escala logarítmica en el eje X para visualizar uniformemente los tramos. Se observa inestabilidad con umbrales muy bajos o muy altos, destacando un punto de equilibrio cercano a los 4096 elementos.}
\label{fig:granularidad}
\end{figure}



\begin{figure}[H]
    \centering
    \begin{tikzpicture}
        \begin{axis}[
            title={Speedup para $N = 2^{26}$},
            xlabel={Número de Hilos ($p$)},
            ylabel={Speedup ($T_1 / T_p$)},
            xmode=log,           % Escala logarítmica en X
            log basis x=2,       % Base 2
            xmin=1, xmax=8,
            ymin=1, ymax=8,
            xtick={1, 2, 4, 8},
            log ticks with fixed point,
            ytick={1, 2, 3, 4, 5, 6, 7, 8},
            legend pos=north west,
            legend cell align={left},
            legend style={
                font=\small,
                fill=white,
                fill opacity=0.85,
                draw=gray!40,
            },
            xmajorgrids=true,
            ymajorgrids=true,
            grid style={dashed, gray!40},
            width=11cm,
            height=8cm,
            line width=1.2pt
        ]
        
        % Ideal teórica
        \addplot[color=gray, dashed, thick, domain=1:8] {x};
        \addlegendentry{Ideal ($S_p = p$)}
        
        % Par_Mergesort
        \addplot[
            color=blue!80!black, 
            mark=square*, 
            mark options={fill=white, solid}
        ] coordinates {
            (1,1.0000) (2,1.9326) (4,2.9488) (8,3.6748)
        };
        \addlegendentry{Par\_Mergesort}
        
        % Full_Paralelo
        \addplot[
            color=green!60!black, 
            mark=*, 
            mark options={fill=white, solid}
        ] coordinates {
            (1,1.0000) (2,1.9113) (4,3.4469) (8,4.9096)
        };
        \addlegendentry{Full\_Paralelo}
        
        \end{axis}
    \end{tikzpicture}
    \caption{Escalabilidad de los algoritmos para el tamaño máximo de arreglo analizado ($N=2^{26}$). Se emplea una escala logarítmica en el eje de las abscisas para distribuir uniformemente los hilos. Se evidencia la caída de pendiente producto de la saturación del ancho de banda y la topología SMT a partir de $p=4$.}
    \label{fig:speedup_n26}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{tikzpicture}
        \begin{axis}[
            title={Eficiencia para $N = 2^{26}$},
            xlabel={Número de Hilos ($p$)},
            ylabel={Eficiencia ($S_p / p$)},
            xmode=log,           % Escala logarítmica en X
            log basis x=2,       % Base 2
            xmin=1, xmax=8,
            ymin=0.35, ymax=1.05,
            xtick={1, 2, 4, 8},
            log ticks with fixed point,
            ytick={0.4, 0.5, 0.6, 0.7, 0.8, 0.9, 1.0},
            legend pos=south west,
            legend cell align={left},
            legend style={
                font=\small,
                fill=white,
                fill opacity=0.85,
                draw=gray!40,
            },
            xmajorgrids=true,
            ymajorgrids=true,
            grid style={dashed, gray!40},
            width=11cm,
            height=8cm,
            line width=1.2pt
        ]
        
        % Ideal teórica
        \addplot[color=gray, dashed, thick, domain=1:8] {1};
        \addlegendentry{Ideal ($E_p = 1.0$)}
        
        % Par_Mergesort
        \addplot[
            color=blue!80!black, 
            mark=square*, 
            mark options={fill=white, solid}
        ] coordinates {
            (1,1.0000) (2,0.9663) (4,0.7372) (8,0.4593)
        };
        \addlegendentry{Par\_Mergesort}
        
        % Full_Paralelo
        \addplot[
            color=green!60!black, 
            mark=*, 
            mark options={fill=white, solid}
        ] coordinates {
            (1,1.0000) (2,0.9557) (4,0.8617) (8,0.6137)
        };
        \addlegendentry{Full\_Paralelo}
        
        \end{axis}
    \end{tikzpicture}
    \caption{Degradación de la eficiencia paralela ($N=2^{26}$). Se emplea una escala logarítmica en el eje de las abscisas para distribuir uniformemente los hilos. La Versión Completa logra mantenerse por encima del 60\% de eficiencia bajo máxima carga, mitigando fuertemente el cuello de botella que castiga al Mergesort Paralelo clásico (el cual cae al 45\%).}
    \label{fig:eficiencia_n26}
\end{figure}

\begin{table}[H]
\centering
\caption{Impacto del parámetro $k$ en el tiempo de ejecución para la carga máxima ($N=2^{26}$).}
\vspace{0.2cm}
\begin{tabular}{c|cc}
\hline
\textbf{Valor de $k$} & \textbf{K-Way Secuencial (s)} & \textbf{Full Paralelo $p=8$ (s)} \\ \hline
$k = 2$ & 8,42 & 1,05 \\
$k = 4$ & 7,10 & 0,85 \\
$k = 8$ & 7,10 & 0,81 \\
$k = 16$ & 7,29 & 0,81 \\ \hline
\end{tabular}
\label{tab:impacto_k}
\end{table}

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
    title={Impacto del valor de $k$ en el tiempo de ejecución ($N=2^{26}$)},
    xlabel={Valor de $k$ (Vías de mezcla en escala logarítmica)},
    ylabel={Tiempo de Ejecución (s)},
    xmode=log,
    log basis x=2,
    xtick={2, 4, 8, 16},
    log ticks with fixed point,
    ymin=0, ymax=10, % Aumentamos el rango para que no se pegue al borde superior
    ytick={0, 2, 4, 6, 8, 10},
    legend style={
        at={(0.5,-0.25)}, % Desplaza la leyenda debajo del gráfico
        anchor=north,
        legend columns=-1, % Disposición horizontal para no tapar datos
        font=\small,
        fill=white,
        fill opacity=0.85,
        draw=gray!40,
    },
    ymajorgrids=true,
    grid style={dashed, gray!40},
    width=11cm,
    height=7cm,
    line width=1.2pt
]

% K-Way Secuencial
\addplot[
    color=red!80!black, 
    mark=square*, 
    mark options={fill=white, solid}
] coordinates {
    (2, 8.42) (4, 7.10) (8, 7.10) (16, 7.29)
};
\addlegendentry{K-Way Secuencial}

% Full Paralelo (p=8)
\addplot[
    color=green!60!black, 
    mark=*, 
    mark options={fill=white, solid}
] coordinates {
    (2, 1.05) (4, 0.85) (8, 0.81) (16, 0.81)
};
\addlegendentry{Full Paralelo ($p=8$)}

\end{axis}
\end{tikzpicture}
\caption{Evolución del tiempo de ejecución al variar la cantidad de particiones $k$. Se evidencia un punto de rendimiento óptimo entre $k=4$ y $k=8$. El ajuste de la leyenda y los límites del eje permite observar con claridad la brecha de rendimiento entre ambas versiones.}
\label{fig:analisis_k_corregido}
\end{figure}

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
    ybar,
    title={Comparativa de Fallos de Caché ($N = 2^{26}, p = 8$)},
    xlabel={Algoritmo},
    ylabel={Millones de Fallos (Cache Misses)},
    symbolic x coords={Quick\_Par, MS\_Par, Full\_Par},
    xtick=data,
    nodes near coords, % Muestra el valor exacto sobre la barra
    nodes near coords align={vertical},
    ymin=0, ymax=110,
    ylabel style={at={(axis description cs:-0.1,.5)},anchor=south},
    width=11cm,
    height=7.5cm,
    bar width=1.5cm,
    enlarge x limits=0.3,
    ymajorgrids=true,
    grid style={dashed, gray!40},
    axis line style={gray!60},
    every node near coord/.append style={font=\footnotesize, /pgf/number format/fixed, /pgf/number format/precision=1}
]

\addplot[
    fill=blue!60!cyan,
    draw=blue!80!black,
    line width=0.8pt
] coordinates {
    (Quick\_Par, 23.3) 
    (MS\_Par, 90.0) 
    (Full\_Par, 61.7)
};

\end{axis}
\end{tikzpicture}
\caption{Comparación del impacto en la jerarquía de memoria para el tamaño máximo de arreglo. Se observa que Quicksort genera $\sim$74\% menos fallos de caché que el Mergesort Paralelo clásico, debido a su naturaleza \textit{in-place} y mejor localidad de datos.}
\label{fig:profiling_cache}
\end{figure}

\begin{table}[H]
\centering
\caption{Métricas de hardware extraídas con \textit{perf} ($N=2^{26}, p=8$).}
\vspace{0.2cm}
\begin{tabular}{l|ccc}
\hline
\textbf{Algoritmo} & \textbf{Fallos de Caché (M)} & \textbf{IPC Estimado} & \textbf{Estado} \\ \hline
Quick\_Par & 23,3 & 1,20 & OK \\
MS\_Par & 90,0 & 5,31 & OK \\
Full\_Par & 61,7 & 1,00 & OK \\ \hline
\end{tabular}
\label{tab:profiling_final}
\end{table}

\section{Discusión de Resultados y Análisis Teórico}

\subsection{Análisis Experimental Secuencial}
Para el análisis secuencial, se evaluaron tamaños de arreglo variando desde $N=2^{20}$ hasta $N=2^{26}$. A lo largo de todo el espectro, el \textbf{Mergesort Clásico (2-way)} demostró ser sistemáticamente más rápido. Por ejemplo, en la carga máxima ($N=2^{26}$), el Mergesort Clásico finalizó en \textbf{6,46 s}, 
mientras que el \textbf{K-Way Secuencial} ($k=4$) demoró \textbf{6,77 s}.
El análisis de telemetría con \textit{perf} revela la razón detrás de esto: aunque el K-Way Secuencial logra mantener un uso más intensivo del procesador (un IPC de 2,26 frente al 1,90 del clásico), 
la cantidad total de instrucciones ejecutadas aumenta drásticamente (de 66,7 mil millones a 82,7 mil millones de instrucciones). 
Esto demuestra que aumentar el valor de $k$ en un entorno de un solo hilo introduce un 
\textit{overhead} algorítmico significativo debido a la gestión múltiple de las particiones durante la etapa de mezcla (merge),
lo cual anula el beneficio de su alto IPC.

\begin{table}[h]
\centering
\caption{Comparación de rendimiento secuencial: Mergesort Clásico vs K-Way Secuencial ($k=4$).}
\vspace{0.2cm}
\begin{tabular}{l|cc|cc}
\hline
\textbf{Tamaño ($N$)} & \multicolumn{2}{c|}{\textbf{Mergesort Clásico}} & \multicolumn{2}{c}{\textbf{K-Way Secuencial}} \\ 
 & \textbf{Tiempo (s)} & \textbf{IPC} & \textbf{Tiempo (s)} & \textbf{IPC} \\ \hline
$2^{20}$ & 0,091 & 1,95 & 0,092 & 2,33 \\
$2^{22}$ & 0,380 & 1,89 & 0,388 & 2,29 \\
$2^{24}$ & 1,554 & 1,91 & 1,609 & 2,28 \\
$2^{26}$ & 6,467 & 1,90 & 6,772 & 2,26 \\ \hline
\end{tabular}
\label{tab:secuenciales}
\end{table}z

\subsection{Análisis de Escalabilidad y Desempeño Práctico}
En la evaluación empírica, se estudió el comportamiento de las implementaciones variando el número de hilos $p \in \{1, 2, 4, 8\}$ bajo cargas de trabajo que se extienden hasta $n = 2^{26}$ elementos ($\approx 256$\,MB). 
Como se evidencia en los datos experimentales y en la Figura \ref{fig:speedup_n26}, el desempeño exhibe un escalamiento sublineal. 
Para la versión \textit{Mergesort Paralelo} (con mezcla secuencial) en $n=2^{26}$, el Speedup alcanza $3.11\times$ para $p=4$ (eficiencia de $0.77$). 
Sin embargo, al incrementar a $p=8$, la curva sufre una atenuación severa, estancándose en un Speedup de $4.51\times$ y degradando la eficiencia a $0.56$. 

Por su parte, la \textit{Versión Paralela Completa} (K-way con Merge por Ranks) demuestra una resiliencia arquitectónica superior ante el incremento de hebras, 
logrando un Speedup de $4.92\times$ para $p=8$. Es imperativo notar que, aunque el tiempo base de ejecución secuencial ($p=1$) del algoritmo completo es superior debido al 
\textit{overhead} intrínseco del cálculo de particiones ($8.68$\,s frente a los $6.68$\,s del \textit{Par\_Mergesort}), su escalabilidad relativa es sistemáticamente más cercana al ideal técnico.

\subsection{Contraste con el Modelo PRAM y Teorema de Brent}
La atenuación observada en el escalamiento halla justificación analítica a través del modelo PRAM. 
En la variante \textit{Par\_Mergesort}, únicamente se exponen a ejecución concurrente las particiones recursivas, relegando la etapa de mezcla a un proceso estrictamente secuencial. En esta arquitectura, el trabajo total es $W(n) = O(n \log n)$, mientras que el camino crítico o 
\textit{Span} queda dominado invariablemente por la fusión del nivel superior del árbol, resultando en $S(n) = O(n)$. 
Aplicando el Teorema de Brent para estimar el tiempo de ejecución paralelo $T_p$:
\begin{equation}
    T_p \leq \frac{W(n)}{p} + S(n) \approx \frac{n \log n}{p} + n
    \label{eq:brent_par_merge}
\end{equation}
El término dominante $O(n)$ en la Ecuación~\ref{eq:brent_par_merge} impone una barrera asintótica infranqueable (Ley de Amdahl), impidiendo un Speedup lineal sin importar la magnitud de $p$.

En contraposición, la \textit{Versión Paralela Completa} paraleliza la operación de fusión mediante el algoritmo de partición por Ranks y búsqueda binaria. Esta técnica mitiga el cuello de botella reduciendo el \textit{Span} del paso de mezcla a $O(\log n)$. 
Al incorporar esta mejora dentro de la estructura de un árbol K-way (asumiendo $k$ constante), el \textit{Span} global del algoritmo colapsa a:
\begin{equation}
    S_{\text{Completo}}(n) = O(\log^2 n)
\end{equation}
Lo cual reescribe la cota de Brent como:
\begin{equation}
    T_p \approx \frac{n \log n}{p} + \log^2 n
    \label{eq:brent_full}
\end{equation}
Puesto que para grandes magnitudes de datos ($n=2^{26}$) la proporción $\log^2 n \ll n$ es evidente, la Ecuación~\ref{eq:brent_full} demuestra formalmente por qué la versión completa preserva una eficiencia de $0.61$ a $p=8$, frente al $0.56$ de la implementación con mezcla secuencial.

\subsection{Comparación de Paralelización: Mergesort vs Quicksort}
Al contrastar las estrategias de paralelización utilizadas en este trabajo frente al algoritmo \textit{Quicksort Paralelo}, emergen diferencias estructurales importantes. Mergesort posee un enfoque \textit{bottom-up} donde el trabajo pesado recae en la etapa de combinación; por esto, su escalabilidad teórica y práctica está estrictamente condicionada a paralelizar la fusión (Merge por Ranks). 
Por el contrario, Quicksort distribuye el trabajo en un enfoque \textit{top-down} durante la partición inicial. 
Esto hace que Quicksort sea \textit{naturalmente más paralelizable} en sus fases tempranas sin requerir operaciones complejas de combinación, pero lo vuelve vulnerable a un mal balance de carga si el pivote no divide equitativamente los datos. 
En ambos algoritmos, no obstante, calibrar empíricamente un umbral de corte (granularidad) es estrictamente necesario para evitar que el 
\textit{overhead} de OpenMP en los niveles profundos de recursión arruine el \textit{Speedup}.

\subsection{Análisis de Hardware, Profiling y Cuellos de Botella}
\begin{table}[H]
    \centering
    \small \caption{Métricas de hardware capturadas con \texttt{perf} para la carga máxima ($N = 2^{26}$) ejecutando con 8 hilos (vs Secuencial a 1 hilo).}
    \label{tab:hardware_metrics}
    \resizebox{\linewidth}{!}{%
    \begin{tabular}{@{}lccc@{}}
        \toprule
        \textbf{Métrica} & \textbf{Secuencial ($p=1$)} & \textbf{Par\_Mergesort ($p=8$)} & \textbf{Full\_Paralelo ($p=8$)} \\ \midrule
        Instrucciones por Ciclo (IPC) & 1,90 & 1,88 & 2,06 \\
        Fallas de Caché (Misses) & 13.230.990 & 63.212.281 & 73.078.893 \\ 
        Referencias a Caché & 729.405.704 & 2.730.216.123 & 4.622.548.884 \\ \bottomrule
    \end{tabular}
    }
\end{table}
Pese a la optimización asintótica garantizada por la partición en Ranks, los resultados empíricos distan del Speedup ideal ($8.0\times$ para $p=8$). 
El análisis de telemetría de hardware extraído mediante \texttt{perf} revela que el rendimiento queda subyugado a limitaciones físicas: la contención del subsistema de memoria y la topología \textit{Simultaneous Multithreading} (SMT).

\paragraph{Saturación del Ancho de Banda y \textit{Cache Thrashing}:} El ordenamiento es fundamentalmente un problema \textit{memory-bound}. 
El volumen de datos procesado excede masivamente la capacidad de la caché L3 del procesador (16\,MB). En la ejecución de la versión secuencial para $n=2^{26}$, 
se registran $\approx 13.2 \times 10^6$ fallas de caché (\textit{Cache Misses}). Al transicionar a ejecución concurrente con $p=8$, este valor sufre un crecimiento exponencial, 
alcanzando $63.2 \times 10^6$ en \textit{Par\_Mergesort} y $73.0 \times 10^6$ en la \textit{Versión Paralela Completa}. Este incremento del $550\%$ es sintomático del \textit{Cache Thrashing}: 
los múltiples hilos compiten agresivamente por el ancho de banda, 
invalidando recíprocamente sus líneas de caché y forzando ciclos de latencia extrema contra la memoria principal (DDR5).


\textbf{Topología SMT y Overhead Algorítmico (IPC):} 
Un hallazgo paradójico en las métricas es que, a pesar de la saturación del bus de memoria, 
el IPC en la Versión Paralela Completa para $n = 2^{26}$ se mantiene en un destacado $2,06$ (superior al $1,90$ secuencial). 
Esto indica que el procesador no está congelado esperando a la RAM. 
Sin embargo, la eficiencia cae al usar 8 hilos debido al \textit{overhead algorítmico}: 
para paralelizar la mezcla, la CPU debe ejecutar millones de instrucciones adicionales (búsquedas binarias del Merge Path y gestión de OpenMP) que no existen en la versión secuencial. 
Por ende, aunque el procesador computa a toda velocidad, 
gran parte de ese esfuerzo se gasta en coordinación concurrente, 
compitiendo simultáneamente con los hilos de SMT por una memoria DDR5 ya estresada.

\section{Conclusiones}

\subsection{Respuestas a la Discusión Final}
\begin{enumerate}
    \item \textbf{¿Cuál fue la mejor alternativa secuencial en sus experimentos?} \\
    La mejor alternativa secuencial fue el \textbf{Mergesort clásico (2-way)}. Consistentemente obtuvo menores tiempos de ejecución frente al K-way Secuencial (6,46 s vs 6,77 s para $N=2^{26}$). Se comprobó que el enfoque K-way ejecutó aproximadamente 16 mil millones de instrucciones extras por el \textit{overhead} de administrar múltiples particiones, haciendo más ineficiente al procesador secuencial a pesar de lograr un mejor IPC.
    
    \item \textbf{¿Cuál fue la mejor alternativa paralela?} \\
    La \textbf{Versión Paralela Completa (K-Way Paralelo con Merge por Ranks)}. Tal como evidencian los datos, paralelizar la fusión rompió el cuello de botella secuencial de Amdahl, logrando un mejor aprovechamiento de los 8 hilos del sistema en comparación al Mergesort Paralelo tradicional.
    
    \item \textbf{¿Qué factores prácticos explican las diferencias con respecto a la complejidad teórica?} \\
    Principalmente la contención por el ancho de banda de la memoria principal (\textit{Memory-Bound / Cache Thrashing}) y la topología SMT del procesador Ryzen. Aunque teóricamente $S(n) = O(\log^2 n)$ asegura escalabilidad casi ideal, en la práctica, forzar a 8 hilos lógicos a sobrescribir constantemente las líneas de caché de Nivel 3 provoca demoras físicas en el hardware que los modelos matemáticos PRAM y de Brent no contemplan.
    
    \item \textbf{¿Implementación recomendada para arreglos grandes en un multicore?} \\
    Se recomienda sin lugar a dudas la \textbf{Versión Paralela Completa (K-Way + Ranks)} ajustando un umbral de corte de granularidad calibrado a la memoria caché de la CPU objetivo (ej. 4096 elementos). Esta implementación asegura escalar el cómputo sin que el \textit{Span} se estanque linealmente al intentar mezclar los niveles superiores del arreglo.
\end{enumerate}

El desarrollo de este proyecto permite concluir que la optimización de algoritmos de ordenamiento en arquitecturas multinúcleo depende críticamente de la reducción del camino crítico (\textit{Span}) en las etapas de combinación. La implementación del algoritmo \textit{K-way} con \textit{Merge} por Ranks demostró ser la estrategia más robusta, logrando romper el cuello de botella asintótico impuesto por la Ley de Amdahl en las variantes de mezcla secuencial. Al transformar la operación de fusión de un proceso $O(n)$ a uno con un \textit{Span} de $O(\log^2 n)$, se alcanzó un Speedup máximo de $5.02\times$ para $n=2^{26}$, validando empíricamente que la exposición de paralelismo en todas las fases del algoritmo es la única vía para mejorar la escalabilidad en problemas de gran volumen.

No obstante, los resultados experimentales subrayan que el rendimiento no solo está gobernado por la complejidad algorítmica, 
sino por las restricciones físicas de la arquitectura del procesador y la jerarquía de memoria. Se observó una degradación sistemática de la eficiencia al transicionar de 4 a 8 hilos de ejecución.
Este fenómeno se atribuye a la naturaleza \textit{memory-bound} del ordenamiento, donde la saturación del ancho de banda de la memoria DDR5 y la contención de recursos en hilos lógicos compartidos (SMT) impiden un escalamiento lineal. 
El incremento masivo en las fallas de caché L3 bajo máxima concurrencia confirma que, en procesadores modernos como el Ryzen 5 8600G, el movimiento de datos es un factor limitante más severo que la capacidad de cómputo bruta.

Finalmente, se determinó que la gestión de la granularidad es un parámetro determinante para la viabilidad práctica de las tareas en OpenMP. 
Un umbral de granularidad fijado en 4096 elementos permitió un equilibrio óptimo entre la explotación del paralelismo y el \textit{overhead} de creación de tareas. 
Una granularidad bien calibrada garantiza que las subinstancias de ordenamiento aprovechen la localidad de referencia en las cachés L1 y L2, 
minimizando la sincronización pasiva y maximizando el IPC. En conclusión, el diseño de software 
paralelo de alto rendimiento exige una visión holística que integre el análisis \textit{Work/Span} con un conocimiento profundo de la topología del hardware subyacente.



\begin{thebibliography}{9}
\bibitem{openmp} 
OpenMP Architecture Review Board. \textit{OpenMP Application Programming Interface, Version 5.2}. 2021.
\bibitem{kumar} 
Grama, A., Gupta, A., Karypis, G., y Kumar, V. \textit{Introduction to Parallel Computing}. Pearson Education, 2003.
\bibitem{amd} 
Advanced Micro Devices (AMD). \textit{Processor Programming Reference (PPR) for AMD Family 19h Model 60h (Ryzen 8000 Series)}. 2024.
\bibitem{mergepath} 
Odeh, S. J., et al. \textit{Merge Path - A Parallel Merge Algorithm for Programmable Graphics Processors}. Proceedings of the 17th ACM SIGPLAN symposium on Principles and Practice of Parallel Programming, 2012.
\end{thebibliography}

\end{document}
